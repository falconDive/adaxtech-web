import styled, { css } from 'styled-components';

import { AdxCol } from '../Base/';

export const AdxAsset = styled.div`
	display: flex;
	flex-direction: row;
	width: 100%;
	height: 100%;
`
export const AdxSto = styled(AdxCol)`
		

	.sub-col {

	}
`