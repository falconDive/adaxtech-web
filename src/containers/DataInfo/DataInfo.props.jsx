import PropTypes from 'prop-types'

export const propTypes = {
    dataInfo: PropTypes.object.isRequired,
    dataInfoData: PropTypes.object.isRequired
}

export const defaultProps = {
    dataInfo: {},
    dataInfoData: {}
}